# 构建上下文排除：镜像内重新安装依赖，绝不把本地文件/密钥带进去
node_modules
**/node_modules
**/.next
**/.data
**/.data-e2e
**/.data-accept
**/data
**/.env
**/.env.*
.git
*.log
.DS_Store

# 与镜像构建无关的重目录，排掉可显著缩小构建上下文：
# 前四个只在注释/文档里被提到；docs 是知识库；e2e 虽是 pnpm workspace 包，
# 但构建期只按 Dockerfile 里显式列出的清单拷各包 package.json，从不读它。
dogfood-output
dsh-image-gen
.heroui-docs
.infinite-canvas-ref
docs
e2e
