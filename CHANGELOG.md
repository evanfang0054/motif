# Changelog

本项目的所有重要变更记录于此文件。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。

## [0.4.0] - 2026-09-19

v0.3.0 以来的第四轮迭代：1 个 PR、23 个提交、77 个文件变更（+2405 / −215）。
主线是**渠道快速接入**：真实支付渠道（#14）与真实邮件发信（#15）——凭据到手后，
运营者只在管理后台配置即可接入，不碰代码、不重启。

### Added

- **支付渠道适配层**（#14）：`server/payment/` 复刻 mailer 模式（工厂 + 现读现构造 + 热生效）。
  易支付协议逐行移植 go-epay（new-api 同款依赖），MD5 签名以权威测试向量锁定、验签恒定时间比较；
  Stripe 走官方 SDK 托管 Checkout Session（`price_data` 内联价格）+ webhook raw-body 验签
- **PAYMENT_CHANNEL 合一开关**（#14）：`mock / epay / stripe` 危险区切换，替换 `MOTIF_BILLING_MODE`；
  非 mock 渠道下模拟支付一律 403；orders 记录创建渠道，切回 mock 窗口期无法用模拟收银台入账真实订单
- **回调安全**（#14）：公开回调端点 IP 频控；验签失败审计降噪（仅命中真实 pending 订单入库）；
  审计保留 180 天下限；`creditPaidOrder` 四态幂等入账（`not_found` ≠ `duplicate`，防真实付款丢单）；
  金额逐分核对，Stripe 入账前校验 `payment_status === 'paid'`
- **套餐配置化**（#14）：币种（不含零小数货币，防按分计价放大 100 倍）与四档价格后台可配；
  金额统一以「分」整数存储与核对；充值弹窗按渠道切换文案与币种符号（两位小数）
- **支付结果页** `/billing/result`（#14）：pending / paid / canceled / error 完整状态机，
  归属校验（非本人一律 404 不泄露存在性）
- **邮件渠道接入引导**（#15）：每个发信渠道内嵌引导卡——分步申请指引 + 入口直链 +
  客户端本地生成二维码；mailer 字段按所选渠道显隐（草稿优先，所见即所填）
- **发送测试邮件**（#15）：root 独占 + 频控 + 成败双审计，底层失败原因（如 SMTP 535）直接回显
- 设置页新增「支付与套餐」分组与 payment 健康检查探针（判据复用渠道工厂）；
  新增 `SITE_URL` 站点地址配置，支付回调与跳转由此拼接

### Fixed

- 「＋ 新任务」此前不创建新任务（误用 ensureTopic 短路）：现在真正新建（或复用空闲空任务）并切换
- 参考图上传改为**暂存制**：上传不再立即进画布（画布空态下模板画廊保持可选），
  点「开始生成」扣费后才转正为画布图参与生成；面板支持查看与移除暂存
- admin 全部列表时间（订单创建/支付、CDK、审计、反馈、生成日志）按查看者本地时区渲染，
  修复 UTC 原样显示导致的时间偏差

## [0.3.0] - 2026-09-18

v0.2.0 以来的第三轮迭代：6 个 PR、55 个提交、104 个文件变更（+8426 / −146）。
主线是**管理后台从地基到全面上线**（#24–#28），并完成品牌视觉升级。

### Added

- **管理后台地基**（#24）：三级角色 `user < admin < root`；服务启动时若库中尚无超级管理员则自动创建并交付
  **随机强密码**（写入 `dataDir/admin-credentials.txt`，权限 600，同时打印到启动日志；幂等依据是数据库而非文件）；
  `/admin` 服务端角色守卫（未登录 / 被禁用 / 角色不足一律 404，不泄露存在性）与 `/api/admin/*` 鉴权（区分 401 与 403）；
  角色保护规则（管理员不可操作或授予超级管理员、系统不允许失去最后一个超级管理员）；
  `admin_audit` 审计表与写入封装；强制改密软提示横幅；工作台顶栏管理入口（仅管理员可见）
- **管理员凭据工具**（#24）：`pnpm admin:reset` 重置超级管理员密码、`pnpm admin:list` 查看管理员账号；
  兼容「加列之前」的旧库（自愈补列）
- 新增环境变量 `MOTIF_ADMIN_EMAIL` / `MOTIF_ADMIN_PASSWORD` / `MOTIF_SKIP_ADMIN_BOOTSTRAP`
- **管理后台运营页**（#26 / #27）：概览看板（六组运营指标直接读额度流水）、用户管理
  （调整额度 / 禁用启用 / 修改角色 / 一次性重置密码）、生成日志（跨用户查询、按天清理——
  仅清理终态记录，不删画布资产与流水）、CDK 管理（页面发放与查询）、订单、反馈处理、
  审计日志（仅超级管理员可见）
- **额度流水表**（#27）：新增 `credit_ledger`，每次额度变动都有来源记录，账目与余额同事务一致
- **系统设置**（#28）：运行配置以 `settings` 表为唯一真相（首启播种、只写不存在的键）；
  密钥类键只写不读、界面回显掩码；数据位置只读展示；危险区开关（验证码直出、Cookie Secure 等）
  二次确认并留痕；保存后 provider / mailer 热重载，无需重启
- 管理后台响应式（#27）：平板 / H5 下侧栏收纳为左侧抽屉，开关收敛为头部右上角图标按钮
- **品牌视觉「Motif 印章」**：新 Logo 替换旧渐变方块（暖米纸底 + 珊瑚手绘母题 M + 印刷打样对位角标），
  呼应「Motif = 母题、参考图成套出图」；按画幅分层——favicon 与顶栏用减法版保证 16px 清晰，
  落地页 CTA 收尾章等大画幅用完整纹样版（含墨色小 m 变奏）；新增 1200×630 OG 分享图；
  品牌概念与用色规范沉淀至 `docs/brand/`

### Fixed

- 配置播种步骤移出 `instrumentation`，修复 dev 环境下所有路由 500（#28）
- 管理后台侧栏撑满内容区高度；列表页补加载态与分页控件，修复长串撑破卡片 / 表格（#27）
- 危险区配色与只读项展示（#28）
- CDK 兑换失败文案区分「不存在 / 已作废 / 已使用」（#26）
- `<html>` 元素的水合属性警告（#24）：防闪烁内联脚本写入 `data-theme`，以及浏览器扩展注入属性，都会触发该警告；
  改由 `<html suppressHydrationWarning>` 覆盖这两类预期改写

### Changed

- worker 每轮现取生图 provider，配置热重载贯通到执行侧；邮件构造器改为吃取值表，
  抽出 `createMailerFromConfig`（#28）
- 存储层（#24）：新增 `settings` 与 `admin_audit` 两张表；`users` +3 列、`cdks` +1 列、`feedback` +3 列，
  沿用既有 `try/catch ALTER` 自愈迁移风格

## [0.2.0] - 2026-09-18

首次发版（0.1.0）以来的第二轮迭代：7 个 PR、37 个提交、30 个文件变更（+1861 / −233）。

### Added

- **设计系统**：引入 `DESIGN.md` 主题规范与 agent 指令文件，全站样式统一迁移至主题色令牌（#1）
- **主题三态切换**：浅色 / 深色 / 跟随系统，首帧内联脚本防闪烁（#1）
- **生成队列可靠性**：服务启动钩子自动拉起 worker；生成消息支持断点续跑，崩溃恢复后按已生成数补齐、防超发（#8）
- **任务会话复用**：新建任务自动复用未使用的空闲空会话，避免空会话堆积，前端防连点（#9）
- **画布批量操作**：框选命中判定、多选与批量删除（几何纯函数 / store / 接口 / 工具栏）（#10）
- **响应式工作台**：顶栏移动端折叠为汉堡菜单、生成面板窄屏可折叠（#11）
- **H5 / 平板深度走查修复**（#13）：
  - 验证码发送就地反馈（触屏视口内可见）、60 秒重发倒计时、密码明文可见切换
  - 额度预警前置：套用模板即提示「需 N 张 vs 当前余额」，面板常驻消耗提示，余额不足时提交自动打开充值弹窗
  - 生成失败持久横幅（不再只靠一闪而过的 toast）、生成进行中画布状态浮层
  - 上传参考图 / 提交生成时无活动任务自动创建，消除隐性前置
- **入口参数**：落地页支持 `?mode=login` / `?mode=register` / `?invite=CODE`

### Changed

- 品牌 UI 文案统一为大写 Motif（顶栏 / 落地页 / 页脚 / 页面标题 / 模拟收银台）
- 失败与退款类 toast 驻留延长至 6 秒，重要信息更易被注意到（#12）
- 主题切换与品牌链接点击目标放大至舒适触屏尺寸（≥40px）（#12）
- 顶栏断点重构：完整操作区 ≥1280 展示，平板档保留充值主操作 + 汉堡菜单，按钮文字 `nowrap` 防折行（#13）
- 落地页保持默认登录模式；「免费注册」「注册送额度」等注册意图 CTA 点击显式切换（#13）

### Fixed

- worker 崩溃恢复后可能重复生成 / 超发额度的问题；`stopWorker` 清理（#8）
- `instrumentation` 改用官方守卫写法，修复 `next dev` 编译原生依赖失败（#9）
- 落地页导航层叠问题与 hero 满屏（`100svh`，移动端地址栏收起不跳动）（#11）
- 画布图片移除后选中集残留「已选 N」（#10）
- H5 余额常显与紧凑文案，测试断言同步兼容（#12）
- 顶栏在平板宽度（768–1023）下按钮文字折行、任务标题截断（#13）

## [0.1.0] - 2026-09-17

### Added

- 落地页与账号体系：邮箱验证码注册 / 登录 / 改密 / 找回密码 / 昵称头像资料
- 工作台：模板画廊（8 套原创模板）、任务系统（增删改查 / 取消退额）、交互画布（拖拽 / 缩放 / 整理布局 / 灯箱 / 浮动工具栏）
- 真实生图网关对接：文生图（images/generations）+ 图生图（images/edits，参考图 multipart）
- 云端任务队列：租约认领、崩溃回收重排、watch 长轮询实时状态
- 额度计费：注册赠送、按张扣费、失败/取消退回、充值套餐、CDK 兑换、邀请奖励
- 验证码邮件：Mailer 抽象（console / SMTP / Resend / SendGrid）
- Docker 一键部署（多阶段构建 + 数据卷持久化 + 健康检查）
- 质量保障：54 个单元测试、ego-browser 端到端（5 轮）与验收套件（A–F）

[Unreleased]: https://github.com/evanfang0054/motif/compare/v0.3.0...HEAD
[0.3.0]: https://github.com/evanfang0054/motif/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/evanfang0054/motif/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/evanfang0054/motif/releases/tag/v0.1.0
