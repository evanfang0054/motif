# Motif 领域词汇表

Motif 的领域只有一条主线：**用户拿参考图 + 提示词，在一个任务里成套出图，按张扣额度。**
本文件只定义词汇与词汇之间的关系，不放实现细节、不放待办、不放设计稿。

## 任务 Topic

一次「参考图 + 提示词 → 成套出图」的工作单元。它聚合若干次生成轮次，以及这块画布上的全部图片。
用户可以重命名、删除、切换任务；一个用户同时可以有多个任务，但同一时刻只有一个「当前任务」。

代码标识 `Topic`；界面文案一律「任务」。

_Avoid_: 话题（仅早期注释残留）、会话、Task、项目

## 当前任务 Active Topic

工作台此刻正在操作的那个任务。生成、上传参考图、画布操作都作用于它。
「新任务」动作的语义是 **find-or-create**：若已存在「空闲且画布上零张图」的任务则复用，否则新建。

_Avoid_: 打开的任务、选中的任务

## 生成轮次 Message

一次生成提交所对应的执行单元。它自带提示词、尺寸、请求张数、参考图列表，产出若干张画布图片。
**终态（已完成 / 失败 / 已取消）归属于生成轮次，不归属于任务。**

代码标识 `Message`。

_Avoid_: 消息（字面直译会误导成 IM 消息）、请求、批次、轮（「轮」在本仓已占用为迭代轮次，如「轮 2」）

## 用户提示词 Prompt 与最终提示词 Final Prompt

`Prompt` 是用户在输入框里写的原文；`Final Prompt` 是实际发给生图网关的提示词——由用户提示词叠加模板/增强指令后得到。
「提示词增强」这一开关决定是否做这层叠加。二者必须区分：界面展示的是前者，排查出图问题要看后者。

_Avoid_: 把两者都叫「提示词」

## 任务状态 TopicStatus

任务上的**活跃态**，回答「这个任务现在有没有在跑」：空闲 `idle` / 排队中 `pending` / 生成中 `running` / 正在停止生成 `canceling`。
任务在生成结束后**回到空闲**，不回落到「已完成 / 失败 / 已取消」。

**当前生效约定**：任务状态实际只取值 `idle` / `pending` / `canceling`；`running` 与终态三态 `completed` / `failed` / `canceled` 虽在类型与展示映射中声明，但无写入路径，属「已声明、暂不可达」（裁决留痕于本地知识库的 ADR 0001，未随仓入库）。

_Avoid_: 用任务状态表达某一次生成的成败；把「已声明」当「会到达」

## 生成轮次状态 MessageStatus

单个生成轮次的状态：排队中 `queued` / 生成中 `running` / 已完成 `completed` / 生成失败 `failed` / 已取消 `canceled`，
以及 `canceling`——取消请求已受理、执行侧尚未确认的中间态。终态一旦写入不再变化。

_Avoid_: 把 `canceling` 当终态（它之后必然走向 `canceled`）

## 画布图片 CanvasImage

任务画布上的一张真实图片，带尺寸、字节数、MIME 与编号。来源只有两种：
`generated`（本轮生成的产物）与 `uploaded`（用户上传）。

_Avoid_: 素材、资产、附件、文件

## 参考图 Reference

**参考图不是独立实体，而是画布图片的一种用法角色。** 生成轮次通过引用一部分画布图片（上传的或已有的）来指定图生图依据；被引用的那张图就在这一轮里成为参考图。
因此「参考图」有归属、有生命周期校验（必须存在、属于同一用户与同一任务），但没有自己的表。

_Avoid_: 把参考图当成独立类型/独立表；把「上传」与「参考」混为一谈（上传是来源，引用是用途）

## 额度 Credit

计费单位。**一张图 = 一个额度。** 额度是用户余额的唯一货币，注册赠送、CDK 兑换、邀请奖励、套餐充值都会增加它，发起生成会减少它。

_Avoid_: 点数、余额（「余额」是额度的数值，不是另一个概念）、积分

## 额度守恒 Credit Conservation

**已扣额度 = 已交付图片数 + 已退回额度**，在任何路径（正常完成、生成失败、用户取消、进程崩溃后重排）都必须成立。
这是验收项，不是优化目标。

_Avoid_: 退额（那是守恒的实现手段之一，不是规则本身）

## 生成租约 Generation Lease

一次生成轮次的**执行权时间窗**。持有租约的执行者才有权推进该轮次；租约到期即视为执行者失联，该轮次可被重新排队。

_Avoid_: 锁（租约会自然到期并自动失效，锁不会）

## 失败重排 Requeue

把执行中断（进程崩溃、租约过期）的生成轮次放回队列重新执行，使其不丢失。
重排必须避免「正在执行中被重排」导致的重复扣费/重复退额。

_Avoid_: 重试（重试是同一执行者再试一次，重排是执行权易主）

## 模板 Template

一套预置的出图配方：主题、分镜角色如何分配到各张图、是否需要参考图、默认张数与尺寸。
模板产出的是**成组**的图，不是单张图。
8 个内置模板已并入**提示词库**的「**系统自带**」源：选中只填提示词，不再联动张数与尺寸。

_Avoid_: 样式、风格、预设、Prompt 模板

## 提示词库 Prompt Library

表单侧检索提示词的弹窗，是提示词的**唯一入口**。支持关键词、标签、来源三种筛选，滚动加载。

_Avoid_: 提示词管理、素材库

## 提示词源 Prompt Source

提示词条目的来源。分两类：**远程源**（可抓取，5 个 GPT 系 GitHub 仓库）与**系统自带**（本地播种，永不抓取）。
清单写死在代码里，库只存抓取状态。

_Avoid_: 渠道、仓库

## 提示词条目 Prompt Entry

一条可填入表单的提示词：标题、描述、标签、示例图 URL 列表。
**只填提示词**——不填张数，也不填尺寸。

_Avoid_: 提示词模板、词条

## 系统自带源 Built-in Source

由代码播种、永不抓取的提示词源。承载 8 个内置**模板**，封面是各自的模板预览图。

_Avoid_: 内置源、默认源

## 套餐 CreditPackage

一次性出售的一定额度组合。币种与各档价格由管理后台配置（默认港元定价），额度张数仍由代码固定。

_Avoid_: 订阅（套餐是一次性购买，无周期性）

## CDK 兑换码

由运营侧离线发放的一次性凭证，兑换后直接增加额度，一个码只能兑换一次。

_Avoid_: 优惠码、代金券（那是折扣，不是额度）

## 订单 Order

一次购买套餐的记录，从 `pending` 走到 `paid`。

_Avoid_: 收据、交易（交易包含支付动作，订单是记录）

## 模拟收银台 Mock Checkout

支付渠道取 `mock` 时的支付路径：走完下单流程但不产生真实扣款。
区分「订单已支付」与「钱真的收到了」——mock 路径下二者不等价；真实渠道凭回调验签入账后才等价。

_Avoid_: 沙箱支付（沙箱是渠道侧概念，这里是应用侧的演示路径）

## 支付渠道 Payment Channel

计费路径的唯一开关（危险区）：`mock` 走模拟收银台；`epay` / `stripe` 走对应真实渠道，即「正式计费」。一个开关同时表达模式与渠道，不存在「live 但未配渠道」的矛盾态。

_Avoid_: 把「计费模式」与「支付渠道」当两个开关；认为真实渠道下模拟收银台仍可用（一律 403）

## 渠道适配层 Payment Gateway

服务端把「下单跳转」与「回调入账」抽象成的统一接口；每个真实渠道一个实现（易支付协议、Stripe），新增渠道 = 新增一个实现。职责：跳转、验签、金额核对；入账始终复用订单的条件更新保持幂等。

_Avoid_: 把渠道实现散落在路由里；在适配层里直接加额度

## 易支付协议 EPay Protocol

第三方聚合收款网关的**协议族泛称**，不是某一家公司：任何遵循该协议的站点或自建网关，都用同一套「网关地址 + 商户 ID + 商户密钥」三件套接入。

_Avoid_: 把易支付当成具体站点名；把协议兼容等同于为某站点背书

## 渠道引导卡 Channel Guide Card

管理后台设置页内嵌的分步申请指引：每个渠道一张卡，含申请入口直链与扫码二维码，把「去哪申请、怎么拿密钥」压到最短。它是静态内容，不是配置项。

_Avoid_: 把引导卡写成数据库配置；在易支付引导卡里推荐具体站点

## 测试发送 Test Send

管理后台用当前生效发信渠道真实投递一封测试邮件的验证动作，失败时透传底层原因（如 SMTP 535 授权码错误）。

_Avoid_: 用测试发送代替真实注册验证码链路验证

## 暂存参考图 Staged Reference

上传后、生成前的参考图状态：先入暂存表，**不进画布**。
点「开始生成」时转正为画布图（来源 uploaded）并参与该轮生成。**上传成功 ≠ 进入画布。**

_Avoid_: 把暂存参考当成画布图片；期待上传后它立即出现在画布上

## 画布 Canvas

任务的展示面（升级后的 topic 自带的那块无限画布），承载该任务的全部画布图片。
**生成入口不在画布上**——画布只负责看、摆、找，出图仍走右侧表单。

代码标识 `canvas_meta`。

_Avoid_: 白板、工作区、舞台

## 画布视口 Viewport

画布面的平移与缩放状态（偏移 + 缩放比），随任务持久化，换设备打开位置一致。

_Avoid_: 镜头、视角、相机

## 画布图片位置 Placement

单张画布图片在画布坐标系里的位置与显示尺寸。冲突按**图片级 LWW** 合并（谁新谁赢），不是整块画布覆盖。

_Avoid_: 坐标、布局、排列

## 画布背景图案 Canvas Pattern

画布底层的点阵或网格线，随视口联动（缩放时间距变化、平移时偏移），默认网格线。
底色始终取中性的 `--canvas-background`。

_Avoid_: 壁纸、贴图

## 画布小地图 Minimap

画布角落的缩略图，显示全部图片的分布与当前视口矩形，点击可跳转。

_Avoid_: 导航图、缩略图

## 画布右键菜单 Context Menu

在画布或图片上右键弹出的操作菜单；定位时贴边翻转，以免被窗口裁掉。

_Avoid_: 悬浮菜单

## 画布快捷键 Shortcuts

画布上的键位映射（撤销/重做、全选、缩放、Esc 等）。**在输入框内不拦截**，避免抢走打字。

_Avoid_: 热键

## 画布归档 Archive

把一块画布导出为 zip（图片 + 一份描述位置的 JSON），以及从 zip 导回。

_Avoid_: 备份、快照

## 新手引导 Onboarding Guide

画布为空时显示的引导卡，给「上传参考图 / 打开提示词库」两个起点。

_Avoid_: 空态、欢迎页

## 双 driver Dual Driver

位置与视口的持久化策略：**本地草稿**（离线可写，刷新不丢）与**云端权威**（跨设备一致）并存，冲突以服务端为准。

_Avoid_: 双写、双存储、本地优先

## 画布补丁 Canvas Patch

一次位置/视口的增量变更描述。服务端按它做图片级 LWW，**补丁里不带删除**（删除走既有删除接口）。

_Avoid_: diff、变更集

## 状态回流 Status Callback

任务在后台跑完时，把终态回执推给正在看别的任务的用户（列表级轮询，仅在有任务在跑时开）。

_Avoid_: 推送、通知

## 血缘连线 Lineage Edge

由**生成轮次**的参考引用推导出的「图 A 是图 B 的参考」的边。它是**推导**出来的只读视图，不落库。
界面文案用「**溯源**」，代码标识用 `lineage`。

_Avoid_: 连线、依赖（那是构建领域的概念）

## 画布血缘层 Lineage Layer

画布世界层内一层只读的 SVG，用来画血缘曲线与同轮次分组框；不参与命中测试。

_Avoid_: 图层

## 版本链 Version Chain

同一**生成轮次**产出的多张图，按序号有序并排。

_Avoid_: 历史、版本历史

## 以它为参考再生成 Regenerate From Image

选中一张图，把右侧表单**预填**成「该轮原始提示词 + 该图作为参考」的动作。
它是**预填**，不是自动重跑——用户改完再自己提交。

_Avoid_: 重试、重跑、一键复刻

## 邀请 Invite

用户持有的专属邀请码/链接。好友经邀请链接注册即建立邀请关系，双方各得额度奖励，奖励人数有上限。

_Avoid_: 推荐、分销

## 角色 Role

账号的权限层级，共三级：普通用户 / 管理员 / 超级管理员。
普通用户只能操作自己的资源；管理员在其之上可查看与治理全站业务资源（用户、兑换码、订单、反馈、生成记录）；超级管理员在管理员之上独占系统级配置与操作留痕的查阅。
一个账号在同一时刻只持有一个角色。

代码标识 `UserRole`；界面文案「管理员」「超级管理员」。

_Avoid_: 权限组、用户组（本仓没有「一组权限」的概念，角色是单值层级）

## 管理员引导 Root Bootstrap

系统在尚无超级管理员时，自动创建第一个超级管理员账号、并为其生成随机强密码的过程。
判定「是否已引导」的依据是**账号是否已存在**，而非凭据是否已交付；因此引导天然幂等，重复启动既不重建账号，也不覆盖已被修改的密码。
凭据只在创建那一刻交付一次，此后只能通过重置入口重新签发。

_Avoid_: 初始化、安装向导、Setup（那些暗示一个需人工走完的交互流程，本过程无人参与）

## 配置播种 Settings Seeding

把部署时以环境变量形式给出的配置，一次性登记为系统自有配置的动作。
播种只发生一次；**播种之后，配置的唯一真相是系统自身**，环境变量的后续变化不再生效。因此播种完成后，「改环境变量」与「改配置」不再是同一件事。

_Avoid_: 配置同步、配置迁移（「同步」暗示双向持续，「迁移」暗示旧源废弃，二者都不准确）

## 热重载 Hot Reload

配置变更后无需重启服务进程，即对**后续**产生的生成轮次生效的能力。
边界：已经开始的生成轮次继续按变更前的配置跑完，不会被中途切换。

_Avoid_: 生效（过于宽泛）、动态配置（描述形态而非动作）

## 危险区 Danger Zone

系统配置中会削弱系统安全基线的开关集合——开启后使原本受保护的入口变得不设防。
与普通配置的差别不在技术要求，而在**后果不可逆且不易察觉**，因此需显式二次确认并留下操作记录。

_Avoid_: 高级设置、开发者选项（二者描述的是面向人群，不是风险性质）

## 管理操作审计 Admin Audit

对管理员与超级管理员在管理后台所做的、会改变他人状态或系统状态的动作的记录。
与生成轮次日志的区别：审计记的是**操作者做了什么**，生成轮次日志记的是**系统跑了什么**。

_Avoid_: 操作日志（与生成轮次日志的「日志」字面重复，易混淆）

## 生成轮次日志 Generation Log

面向运营视角、跨用户查看全部生成轮次的视图。它不产生新数据，只是把既有的生成轮次记录换一个维度（按用户、状态、时间）呈现。

_Avoid_: 系统日志、接口日志（本仓没有这些概念）

## Relationships

- 一个用户持有多个**任务**；同一时刻只有一个**当前任务**
- 一个**任务**包含多次**生成轮次**，以及一块**画布**上的多张**画布图片**
- 一次**生成轮次**引用 0..n 张**画布图片**作为**参考图**，并产出 0..n 张 `generated` 画布图片
- 一次**生成轮次**扣 `请求张数` 个**额度**；终态后按「请求张数 − 已交付张数」退回
- **任务状态**由当前**生成轮次状态**推导：任务回到空闲，终态留在轮次上
- 一次**生成轮次**在被执行期间持有一份**生成租约**；租约过期触发**失败重排**
- **模板**决定一次**生成轮次**的默认张数、尺寸与提示词叠加内容
- **订单**、**CDK 兑换码**、**邀请**、注册赠送都只增不减地向用户的**额度**充值
- 一个**用户**在同一时刻只持有一个**角色**
- **管理员引导**在系统尚无超级管理员时，创建第一个超级管理员
- 只有**角色**为管理员或超级管理员的**用户**可进入管理后台
- **配置播种**把环境变量登记为系统自有配置；此后对配置的变更经**热重载**作用于后续**生成轮次**
- **危险区**是系统配置中会削弱安全基线的子集
- **管理操作审计**记录管理员对**用户**、**CDK 兑换码**、**订单**、**反馈**与系统配置的变更
- **生成轮次日志**是**生成轮次**面向运营的全站视图
- 管理员可调整用户**额度**、切换用户启用状态；禁用不中止在跑的**生成轮次**，因此不触碰**额度守恒**

- 一个**任务**有一块**画布**；**画布图片**各有自己的**画布图片位置**
- **血缘连线**由**生成轮次**的参考引用推导；同一轮次的产出构成**版本链**
- **提示词库**含多个**提示词源**，源含多个**提示词条目**；「**系统自带源**」由代码播种

> **待裁**：`CanvasImage` 的 `_Avoid_` 明确排除「素材」，而延后的「素材库」主题需要一个不冲突的词。
> 本文件暂不引入「素材」作为领域词。
