# Motif 领域词汇表

Motif 的领域只有一条主线：**用户拿参考图 + 提示词，在一个任务里成套出图，按张扣额度。**
本文件只定义词汇与词汇之间的关系，不放实现细节、不放待办、不放设计稿。

## 任务 Topic

一次「参考图 + 提示词 → 成套出图」的工作单元。它聚合若干次生成轮次，以及这块画布上的全部图片。
用户可以重命名、删除、切换任务；一个用户同时可以有多个任务，但同一时刻只有一个「当前任务」。

代码标识 `Topic`；界面文案一律「任务」。

_Avoid_: 话题（仅早期注释残留）、会话、Task、项目

## 当前任务 Active Topic

工作台此刻正在操作的那个任务。生成、上传参考图、画布操作都作用于它。
「新任务」动作的语义是 **find-or-create**：若已存在「空闲且画布上零张图」的任务则复用，否则新建。

_Avoid_: 打开的任务、选中的任务

## 生成轮次 Message

一次生成提交所对应的执行单元。它自带提示词、尺寸、请求张数、参考图列表，产出若干张画布图片。
**终态（已完成 / 失败 / 已取消）归属于生成轮次，不归属于任务。**

代码标识 `Message`。

_Avoid_: 消息（字面直译会误导成 IM 消息）、请求、批次、轮（「轮」在本仓已占用为迭代轮次，如「轮 2」）

## 用户提示词 Prompt 与最终提示词 Final Prompt

`Prompt` 是用户在输入框里写的原文；`Final Prompt` 是实际发给生图网关的提示词——由用户提示词叠加模板/增强指令后得到。
「提示词增强」这一开关决定是否做这层叠加。二者必须区分：界面展示的是前者，排查出图问题要看后者。

_Avoid_: 把两者都叫「提示词」

## 任务状态 TopicStatus

任务上的**活跃态**，回答「这个任务现在有没有在跑」：空闲 `idle` / 排队中 `pending` / 生成中 `running` / 正在停止生成 `canceling`。
任务在生成结束后**回到空闲**，不回落到「已完成 / 失败 / 已取消」。

**当前生效约定**：任务状态实际只取值 `idle` / `pending` / `canceling`；`running` 与终态三态 `completed` / `failed` / `canceled` 虽在类型与展示映射中声明，但无写入路径，属「已声明、暂不可达」（裁决留痕于本地知识库的 ADR 0001，未随仓入库）。

_Avoid_: 用任务状态表达某一次生成的成败；把「已声明」当「会到达」

## 生成轮次状态 MessageStatus

单个生成轮次的状态：排队中 `queued` / 生成中 `running` / 已完成 `completed` / 生成失败 `failed` / 已取消 `canceled`，
以及 `canceling`——取消请求已受理、执行侧尚未确认的中间态。终态一旦写入不再变化。

_Avoid_: 把 `canceling` 当终态（它之后必然走向 `canceled`）

## 画布图片 CanvasImage

任务画布上的一张真实图片，带尺寸、字节数、MIME 与编号。来源只有两种：
`generated`（本轮生成的产物）与 `uploaded`（用户上传）。

_Avoid_: 素材、资产、附件、文件

## 参考图 Reference

**参考图不是独立实体，而是画布图片的一种用法角色。** 生成轮次通过引用一部分画布图片（上传的或已有的）来指定图生图依据；被引用的那张图就在这一轮里成为参考图。
因此「参考图」有归属、有生命周期校验（必须存在、属于同一用户与同一任务），但没有自己的表。

_Avoid_: 把参考图当成独立类型/独立表；把「上传」与「参考」混为一谈（上传是来源，引用是用途）

## 额度 Credit

计费单位。**一张图 = 一个额度。** 额度是用户余额的唯一货币，注册赠送、CDK 兑换、邀请奖励、套餐充值都会增加它，发起生成会减少它。

_Avoid_: 点数、余额（「余额」是额度的数值，不是另一个概念）、积分

## 额度守恒 Credit Conservation

**已扣额度 = 已交付图片数 + 已退回额度**，在任何路径（正常完成、生成失败、用户取消、进程崩溃后重排）都必须成立。
这是验收项，不是优化目标。

_Avoid_: 退额（那是守恒的实现手段之一，不是规则本身）

## 生成租约 Generation Lease

一次生成轮次的**执行权时间窗**。持有租约的执行者才有权推进该轮次；租约到期即视为执行者失联，该轮次可被重新排队。

_Avoid_: 锁（租约会自然到期并自动失效，锁不会）

## 失败重排 Requeue

把执行中断（进程崩溃、租约过期）的生成轮次放回队列重新执行，使其不丢失。
重排必须避免「正在执行中被重排」导致的重复扣费/重复退额。

_Avoid_: 重试（重试是同一执行者再试一次，重排是执行权易主）

## 模板 Template

一套预置的出图配方：主题、分镜角色如何分配到各张图、是否需要参考图、默认张数与尺寸。
模板产出的是**成组**的图，不是单张图。

_Avoid_: 样式、风格、预设、Prompt 模板

## 套餐 CreditPackage

一次性出售的一定额度组合，含港元定价。

_Avoid_: 订阅（套餐是一次性购买，无周期性）

## CDK 兑换码

由运营侧离线发放的一次性凭证，兑换后直接增加额度，一个码只能兑换一次。

_Avoid_: 优惠码、代金券（那是折扣，不是额度）

## 订单 Order

一次购买套餐的记录，从 `pending` 走到 `paid`。

_Avoid_: 收据、交易（交易包含支付动作，订单是记录）

## 模拟收银台 Mock Checkout

当前唯一可用的支付路径：走完下单流程但不产生真实扣款。真实支付渠道未接入。
区分「订单已支付」与「钱真的收到了」——当前二者不等价。

_Avoid_: 沙箱支付（沙箱是渠道侧概念，这里是应用侧的演示路径）

## 邀请 Invite

用户持有的专属邀请码/链接。好友经邀请链接注册即建立邀请关系，双方各得额度奖励，奖励人数有上限。

_Avoid_: 推荐、分销

## Relationships

- 一个用户持有多个**任务**；同一时刻只有一个**当前任务**
- 一个**任务**包含多次**生成轮次**，以及一块**画布**上的多张**画布图片**
- 一次**生成轮次**引用 0..n 张**画布图片**作为**参考图**，并产出 0..n 张 `generated` 画布图片
- 一次**生成轮次**扣 `请求张数` 个**额度**；终态后按「请求张数 − 已交付张数」退回
- **任务状态**由当前**生成轮次状态**推导：任务回到空闲，终态留在轮次上
- 一次**生成轮次**在被执行期间持有一份**生成租约**；租约过期触发**失败重排**
- **模板**决定一次**生成轮次**的默认张数、尺寸与提示词叠加内容
- **订单**、**CDK 兑换码**、**邀请**、注册赠送都只增不减地向用户的**额度**充值
