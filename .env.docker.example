# Motif Docker 部署配置模板
#
#   cp .env.docker.example .env   &&   编辑 .env 填好下面两项   &&   docker compose up -d --build
#
# ⚠️ 这些值只在**首次启动**时播种进数据库（settings 表），之后改这里不再生效 ——
#    要改请走「管理后台 → 系统设置」（保存即热生效，无需重启）。
#    例外：MOTIF_DATA_DIR / MOTIF_DB_FILE 与引导类参数只认环境变量。
# ⚠️ 本文件是模板，不要在这里写真实密钥后提交（`.env` 已被 .gitignore / .dockerignore 排除）。

# —— 必需：你的 OpenAI 兼容生图网关 ——
# 例：https://api.openai.com/v1、自建网关 http://gateway:3000/v1
IMAGE_API_BASE_URL=
# 网关令牌（形如 sk-...）
IMAGE_API_KEY=
# 模型名，默认 gpt-image-2
# IMAGE_MODEL=gpt-image-2

# —— 推荐：站点对外地址（支付回调 / 支付跳转 / 找回密码邮件直达链接都要它）——
# SITE_URL=https://motif.example.com

# —— HTTPS 部署时开启 ——
# MOTIF_COOKIE_SECURE=1

# —— 图片存储切 S3 / MinIO（默认 local）——
# STORAGE_DRIVER=s3
# S3_ENDPOINT=https://s3.example.com
# S3_BUCKET=motif
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_FORCE_PATH_STYLE=true
# S3_PUBLIC_BASE_URL=

# —— 邮件发信（默认 console：验证码打进容器日志）——
# MOTIF_MAILER=smtp
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=

# —— 仅本地联调：注册验证码随接口直出。生产务必保持注释 ——
# MOTIF_EXPOSE_DEV_CODE=1
